{
  "name": "functions",
  "description": "Cloud Functions for Project Modern",
  "scripts": {
    "lint": "eslint .",
    "dev": "nodemon .",
    "serve": "firebase serve --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "jest --watchAll --verbose --testTimeout=10000",
    "preflight": "jest --testTimeout=10000"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "firebase-admin": "^8.12.1",
    "firebase-functions": "^3.6.2",
    "helmet": "^3.22.0"
  },
  "devDependencies": {
    "eslint": "^7.1.0",
    "eslint-plugin-promise": "^4.2.1",
    "jest": "^26.0.1",
    "nodemon": "^2.0.4",
    "supertest": "^4.0.2"
  },
  "engines": {
    "node": "8"
  },
  "private": true,
  "jest": {
    "testEnvironment": "node"
  }
}
